import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  item: {
    height: 250,
    marginBottom: 30,
    backgroundColor: '#40e0be',
  },

  lottie: {
    width: '100%',
    height: '100%',
  },
});
